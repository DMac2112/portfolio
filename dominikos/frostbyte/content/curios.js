// content/curios.js — world discovery registry. W0 ships the system empty; room entries land with
// their area phase so ids/labels become contracts only when their actual clickable exists.
export const CURIO_REGISTRY = Object.freeze([]);
